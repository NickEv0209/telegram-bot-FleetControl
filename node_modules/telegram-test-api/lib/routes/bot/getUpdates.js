"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getUpdates = void 0;
const utils_1 = require("./utils");
const getUpdates = (app, telegramServer) => {
    (0, utils_1.handle)(app, '/bot:token/getUpdates', (req, res, _next) => {
        const botToken = req.params.token;
        const data = { ok: true, result: telegramServer.getUpdates(botToken) };
        res.sendResult(data);
    });
};
exports.getUpdates = getUpdates;
