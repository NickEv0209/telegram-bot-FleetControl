"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.sendMessage = void 0;
const utils_1 = require("./utils");
const sendMessage = (app, telegramServer) => {
    (0, utils_1.handle)(app, '/bot:token/sendMessage', (req, res, _next) => {
        const botToken = req.params.token;
        const result = telegramServer.addBotMessage(req.body, botToken);
        const data = { ok: true, result };
        res.sendResult(data);
    });
};
exports.sendMessage = sendMessage;
